/**
 * Common device sizes for window
 */

export const IMG_DEVICE_SIZES = [768, 1200, 1800];

/**
 * The density of the screen.
 * (retina would be 2, standard woudl be 1)
 */

// Halfway through standard and retna
export const IMG_SCALING = 1.5;

/**
 * Default quality
 * Sets the default quality of an image unless specified
 */

export const DEFAULT_QUALITY = 75;
